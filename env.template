# PostgreSQL Database Connection Configuration Template
# Copy this file to .env and update with your actual database credentials

# Database host (e.g., localhost, 127.0.0.1, or remote server IP)
DB_HOST=localhost

# Database port (default PostgreSQL port is 5432)
DB_PORT=5432

# Database name
DB_NAME=your_database_name

# Database username
DB_USER=your_username

# Database password
DB_PASSWORD=your_password
